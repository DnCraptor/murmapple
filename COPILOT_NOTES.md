# Copilot workspace notes

## Important

- **YOU DON'T FLASH — I FLASH.**
  - Do not run `./flash.sh` or any `picotool load/reboot` commands.
  - Only build artifacts (e.g., `./build.sh`) and tell the user what to flash.
